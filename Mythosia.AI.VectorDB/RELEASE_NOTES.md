# Mythosia.AI.VectorDB - Release Notes

## v1.1.0

### Changed

- Updated `PackageProjectUrl` to new Mythosia.AI repository.
- Added README packaging support for NuGet.

---

## v1.0.0

### Initial Release

- `InMemoryVectorStore` with cosine similarity TopK search.
- Thread-safe concurrent access via `ConcurrentDictionary`.
- Namespace isolation and metadata filtering.
- Minimum score threshold support.
- Single and batch upsert/delete operations.
- Collection management (create, check, delete).
